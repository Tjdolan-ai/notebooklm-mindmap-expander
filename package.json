{
  "name": "notebooklm-mindmap-expander",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "mkdir -p dist && esbuild src/expander.ts --bundle --outfile=dist/expander.js && esbuild src/background.ts --bundle --outfile=dist/background.js && esbuild src/options.ts --bundle --outfile=dist/options.js",
    "test": "vitest"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.0",
    "esbuild": "^0.23.0",
    "typescript": "^5.5.4",
    "vitest": "^2.0.4"
  },
  "dependencies": {
    "happy-dom": "^18.0.1"
  }
}
