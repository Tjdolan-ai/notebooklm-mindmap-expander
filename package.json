{
  "name": "notebooklm-mindmap-expander",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "esbuild src/expander.ts --bundle --outfile=dist/expander.js; esbuild src/background.ts --bundle --outfile=dist/background.js; esbuild src/options.ts --bundle --outfile=dist/options.js",
    "test": "vitest"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.0",
    "esbuild": "^0.25.6",
    "typescript": "^5.5.0",
    "vitest": "^1.5.0"
  }
}
