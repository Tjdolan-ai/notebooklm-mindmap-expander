{
  "name": "notebooklm-mindmap-expander",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "esbuild src/expander.ts --bundle --outfile=dist/expander.js",
    "test": "vitest"
  },
  "devDependencies": {
    "esbuild": "^0.25.6",
    "typescript": "^5.5.0",
    "vitest": "^1.5.0"
  }
}